package days25;

import java.util.Arrays;

/**
 * @author Yelin
 * @date 2022. 3. 24. - 오후 12:20:59
 * @subject p624 Arrays 컬렉션 클래스 : 배열을 좀 더 쉽게 다루는 기능이 구현된 클래스
 * @content
 */
public class Ex08 {

	public static void main(String[] args) {
		// 배열을 쉽게 출력하는 메서드
		// Arrays.toString(null);

		// 모두 하나의 값으로 채워넣는 메서드
		// Arrays.fill(null, false);
		// Arrays.setAll(null, null);
		
		// 배열을 정렬하는 메서드
		// Arrays.sort(null);
		
		// 배열 안에 있는 값을 검색하는 메서드
		// Arrays.binarySearch(null, 0);
		
		// 배열을 List에 담아서 반환하는 메서드
		// Arrays.asList(Object... a);
		
		// 배열 복사하는 메서드
		// Arrays.copyOf(), Arrays.copyOfRange()
		
		// 배열을 비교하는 메서드
		// Arrays.equals(null, null);
		
	} // main

} // class
